import PropBoxLayout from "./PropBoxLayout";

const SelectPropBox = () => {
    return (
        <PropBoxLayout>
            오브젝트를 선택하면 속성을 변경할 수 있습니다.
        </PropBoxLayout>
    )
}

export default SelectPropBox;